import numpy as np
import numba
import matplotlib.pyplot as plt


def dla_monte_carlo(N):
    """DLA model using Monte Carlo simulation

    Args:
        N (int): lattice size
    """
    print("DLA Monte Carlo model.")

    return
